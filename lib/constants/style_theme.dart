import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class PageStyle {
  static Color mainColor = Colors.white;
  static Color backGroundColor = Colors.black54;

  static Color edgeMainColor = Colors.white.withValues(alpha: .5);
  static Color mainColorOpposite = Colors.black;
  static Color accentColor = Colors.greenAccent;
  static Color scrollbarColor = Colors.white;
  static Color panelBorderColor = Colors.black;
  static Color hoverColor = Colors.white;
  static Color scrollbarThumbColor =Colors.white.withValues(alpha: .5);
}

class DrawerStyle {
 static const double widthExpanded = 250;
  static const double widthExpandedPadding8 = 258;
  static const double widthExpandedPadding16 = 266;

  static const int animationDuration = 300;
}